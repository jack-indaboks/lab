[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lab"
version = "0.0.0"
description = "Lab control layer"
requires-python = ">=3.11"

[project.scripts]
lab = "lab.cli:main"
